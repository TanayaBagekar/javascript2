


function students(){
    let a=40;
     let  b=80;
    if(a > b){   
    console.log("good student");
    }


    else (a > b)
    {
        console.log("bad student");
    }


function persentage(){
 let persentage =50;
 let persentage1=60;
 let persentage2=70;
 let persentage3=80;

    if(persentage1 < persentage2 && 50 <60){
        console.log('third class');
    }
    else if( persentage2 < persentage3 && 60 < 70){
        console.log('2nd class');
    }
    else if(persentage3 > persentage1 && 70 >50){
        console.log('topper');
    }
    else (persentage3 > persentage)
    {
        console.log('1st class');
    }
}
 //T || T=T; T || F= T ; F || T = T ; F || F = F
    function logicalOr() {
        if( 10 >= 5 || 10 < 50){
            console.log('inside if');
        }
        
    }
}
    // ternory operator= condition == true? true: false
    50 > 30 || 50 >=20? console.log('50 is greater'): console.log('30 is greater');
    

    //prefix = ++i :first value will get increment and, then will return
    // postfix = i++ operator: first retturn value then  will increment by 1
    let i=0;
    console.log( i );
    console.log('i++',i++);
    console.log("i",i);

    let j=1;
    rest= j +1;
    console.log('j',j);

    let h=1;
    console.log('h',h);
    console.log('h++',h++);
    console.log('h',h);

    let tanaya =30;
    let shrushti =40;
    let result = tanaya + shrushti;
    console.log('result', result);

    let a =40;
    let b =30;
    let resul= a * b;
    console.log('result', resul);

    let check=40;
    let ba =30;
    let resu= check++;
    console.log('result', resu);

    let tanu = 40;
    let y=80;
    let v= tanu--;
    console.log('result', v);

    let t=30;
    let s=40;
    let r= ++t;
    console.log('result',r);

    tanaya ==shrushti? console.log('no is equal'):console.log('np. is not equal');
